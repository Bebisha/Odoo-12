<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_category_property_form" model="ir.ui.view">
        <field name="name">partner.over.credit.limit.form.inherit</field>
        <field name="model">res.partner</field>
        <field name="type">form</field>
        <field name="inherit_id" ref="account.view_partner_property_form"/>
        <field name="arch" type="xml">
            <field name="property_payment_term_id" position="after">
                <field name="over_credit"
                       attrs="{'invisible': [('customer', '=', False)]}"
                       groups='customer_credit_limit.group_credit_limit'/>
                <field name="credit_limit" attrs="{'invisible': ['|',('customer', '=', False),('over_credit', '=', False)]}" groups="customer_credit_limit.group_credit_limit"/>
                <field name="available_credit_limit"
                       attrs="{'invisible': ['|',('customer', '=', False),('over_credit', '=', False)]}" />
                <field name="sale_amount" invisible='1'/>
            </field>
        </field>
    </record>
</odoo>
